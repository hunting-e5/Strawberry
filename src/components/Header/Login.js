import React from 'react';

function Login() {
    return (
        <span id = 'login_wrap'>
            <button>로그인</button>
            <button>회원가입</button>
        </span>
    );
}

export default Login;